# JSON

`json.Marshal`/`MarshalIndent`, `json.Unmarshal`, struct tags (`json:"name"`), `omitempty`, serta custom `MarshalJSON`/`UnmarshalJSON`.

## Cara Menjalankan

```bash
go run -tags=json ./stdlib/
```

## Source Code

```go
{{#include ../../stdlib/json.go}}
```
