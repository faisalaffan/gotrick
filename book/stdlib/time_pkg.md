# Time

`time.Now`, `time.Date`, `Format` (layout "2006-01-02 15:04:05"), `Parse`, `Since`/`Until`, `Ticker` (periodic), serta `Timer`/`After` (delay/select timeout).

## Cara Menjalankan

```bash
go run ./stdlib/time_pkg/ ./stdlib/
```

## Source Code

```go
{{#include ../../stdlib/time_pkg/main.go}}
```
