# I/O & Bufio

Custom `io.Reader` implementation, `io.Copy`, `bufio.Scanner` (baca baris), `bufio.Writer`, `io.TeeReader`, dan `io.LimitReader`.

## Cara Menjalankan

```bash
go run -tags=io_bufio ./stdlib/
```

## Source Code

```go
{{#include ../../stdlib/io_bufio.go}}
```
